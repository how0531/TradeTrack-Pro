/**
 * [DEPRECATED] 此檔案僅用於向後相容
 * 
 * 新程式碼請直接從 src/types/ 目錄匯入：
 * import { Trade, BrokerConfig } from './types';
 * 
 * 舊程式碼的匯入不受影響：
 * import { Trade } from './types';
 */

// Re-export all types for backward compatibility
export * from './types/index';
